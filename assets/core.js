(function(root,factory){const api=factory();if(typeof module==='object'&&module.exports)module.exports=api;else root.BriefCore=api;})(typeof globalThis!=='undefined'?globalThis:this,function(){
 'use strict';
 const safeNumber=x=>Number.isFinite(Number(x))&&Number(x)>=0?Number(x):0;
 function filterItems(items,query={}){const search=String(query.search||'').trim().toLocaleLowerCase();return items.filter(i=>(!query.market||query.market==='all'||(i.markets||[]).includes(query.market))&&(!query.topic||query.topic==='all'||i.topic===query.topic)&&(!query.direction||query.direction==='all'||i.human_direction===query.direction)&&(!search||[i.headline,i.deck,i.publisher,i.creator,i.creator_origin].join(' ').toLocaleLowerCase().includes(search)));}
 function sortItems(items,order='latest',metrics={}){return [...items].sort((a,b)=>{if(order==='daily'){const d=safeNumber(a.daily_rank)-safeNumber(b.daily_rank);if(d)return d;}if(['reads','likes','comments'].includes(order)){const d=safeNumber(metrics[b.key]?.[order])-safeNumber(metrics[a.key]?.[order]);if(d)return d;}return String(b.date).localeCompare(String(a.date))||a.key.localeCompare(b.key);});}
 function shareLinks(url,title){const u=encodeURIComponent(url),t=encodeURIComponent(title);return {whatsapp:'https://wa.me/?text='+t+'%20'+u,linkedin:'https://www.linkedin.com/sharing/share-offsite/?url='+u,facebook:'https://www.facebook.com/sharer/sharer.php?u='+u,bluesky:'https://bsky.app/intent/compose?text='+t+'%20'+u,x:'https://twitter.com/intent/tweet?url='+u+'&text='+t,reddit:'https://www.reddit.com/submit?url='+u+'&title='+t,telegram:'https://t.me/share/url?url='+u+'&text='+t,email:'mailto:?subject='+t+'&body='+u};}
 function consentValid(c,now=Date.now()){return !!c&&c.version===1&&typeof c.analytics==='boolean'&&Number.isFinite(c.at)&&now-c.at<180*86400000&&c.at<=now;}
 function qualifiedRead(seconds,progress,consent){return consent===true&&seconds>=20&&seconds<=3600&&progress>=.5&&progress<=1;}
 function publicURL(config={},item={},locationHref=''){if(!item.path||!config||config.is_preview)return null;if(config.site_url){const u=new URL(config.site_url.replace(/\/$/,'')+'/'+item.path.replace(/index\.html$/,''));return u.protocol==='https:'?u.href:null;}if(!/^https?:/.test(locationHref))return null;return new URL(item.path,locationHref).href;}
 return {filterItems,sortItems,shareLinks,consentValid,qualifiedRead,publicURL,safeNumber};
});
