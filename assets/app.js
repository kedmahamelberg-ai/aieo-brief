"use strict";
document.querySelectorAll("[data-action]").forEach((button)=>button.addEventListener("click",async()=>{const action=button.dataset.action;if(action==="share"){if(navigator.share){try{await navigator.share({title:document.title,url:location.href});return}catch(e){}}try{await navigator.clipboard.writeText(location.href);const old=button.textContent;button.textContent="✓ Link copied";setTimeout(()=>button.textContent=old,1500);return}catch(e){}}const old=button.textContent;button.textContent="Coming in the community phase";setTimeout(()=>button.textContent=old,1700)}));
